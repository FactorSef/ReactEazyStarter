import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, text } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { Test } from "./index";

<Meta title="Test Stories/MDX" decorators={[withKnobs]} />

# Test

With `MDX` we can define a story for `Test` right in the middle of our
markdown documentation.

<Preview>
	<Story name="Test">
		<Test text={text("text", "Hello world")} onClick={action("Test void event")} />
	</Story>
</Preview>

# Props

<Props of={Test} />
